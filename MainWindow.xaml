<Window x:Class="VS_to_VSC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VS_to_VSC"
        mc:Ignorable="d"
        Title="Visual Studio to VSCode Converter" Height="450" Width="700"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- ファイルパス入力フィールド -->
        <TextBox x:Name="FilePathTextBox" 
                 Grid.Row="0" Grid.Column="0"
                 Margin="0,0,15,0"
                 Height="32"
                 VerticalAlignment="Center"
                 Text=""
                 FontSize="12"
                 Padding="8,6"
                 ToolTip="Visual Studioソリューションファイルのパスを入力してください。Enterキーで変換開始"
                 KeyDown="FilePathTextBox_KeyDown"/>

        <!-- 参照して変換ボタン -->
        <Button x:Name="BrowseButton" 
                Grid.Row="0" Grid.Column="1"
                Content="参照して変換" 
                Width="120" Height="32"
                Click="BrowseButton_Click"
                FontSize="12"
                FontWeight="Medium"
                ToolTip="ソリューションファイルを選択して変換開始"/>

        <!-- ログ表示エリア -->
        <TextBox x:Name="LogTextBox" 
                 Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Text=""
                 FontFamily="Consolas"
                 FontSize="11"
                 Background="#2D2D30"
                 Foreground="White"
                 BorderBrush="#3F3F46"
                 BorderThickness="1"
                 Padding="12,10"/>
    </Grid>
</Window>
